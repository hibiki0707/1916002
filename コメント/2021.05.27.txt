#include書くと、そのファイルが#include分の一にコピー（置換）されるので
原則的にヘッダファイルの中で#includeは・・・しないように
もしほかの型を使用する必要がある場合はプロトコル宣言で
何とかならないか検討する。

（プロトタイプ宣言だけでは、内部に「「実体」は持てないので注意）
メンバ変数として持てるのは、ポインタか、参照だけ

ただし、基本的な型（Vector2/3系とかの幾何学系）C++標準系（vector/map/algo)
あと、インクルードしないと、
